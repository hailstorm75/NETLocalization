<Page
    x:Class="Exp.WinUI.Views.MainPageViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winUi="using:Localization.WinUI"
    xmlns:local="using:Exp.WinUI"
    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
    mc:Ignorable="d">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ComboBox
          Grid.Row="0"
          DisplayMemberPath="DisplayName"
          ItemsSource="{Binding Languages}"
          SelectedItem="{Binding Language}" />

        <TextBlock Grid.Row="1" Text="{winUi:Tr String={x:Bind local:Provider.Greeting}}" />
        <TextBlock Grid.Row="2" Text="{winUi:Tr String={x:Bind local:Provider.Farewell}}" />
    </Grid>
</Page>
